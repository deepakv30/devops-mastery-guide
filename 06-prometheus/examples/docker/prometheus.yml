global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Optional second job. Uncomment if you start node-exporter in docker-compose.yml.
  # - job_name: node
  #   static_configs:
  #     - targets: ["node-exporter:9100"]
